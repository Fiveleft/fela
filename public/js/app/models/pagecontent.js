// Filename: models/pagecontent.js
define(
  ['underscore','backbone'], 
  function(_, Backbone) {
    var PageContent = Backbone.Model.extend({
      // initialize: function() {}
    });
    // Return the model for the module
    return PageContent;
  });